export type EmployeeDTO = {
  id: number;
  firstName: string;
  lastName: string;
};

export type CreateEmployeeDTO = {
  firstName: string;
  lastName: string;
};
export type UpdateEmployeeDTO = {
  id: number;
  firstName: string;
  lastName: string;
};
