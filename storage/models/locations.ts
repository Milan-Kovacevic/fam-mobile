export type LocationDTO = {
  id: number;
  name: string;
  latitude: number;
  longitude: number;
};

export type CreateLocationDTO = {
  name: string;
  latitude: number;
  longitude: number;
};
export type UpdateLocationDTO = {
  id: number;
  name: string;
  latitude: number;
  longitude: number;
};
